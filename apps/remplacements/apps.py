from django.apps import AppConfig


class RemplacementsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.remplacements'
    verbose_name = 'Gestion des remplacements'

